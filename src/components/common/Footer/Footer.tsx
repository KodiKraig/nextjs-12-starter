import { Container } from '@components/common';
import { Text } from '@components/ui';

const Footer = () => {
  return (
    <footer className="bg-secondary text-secondary pt-12">
      <Container>
        <Text>Hello world</Text>
      </Container>
    </footer>
  );
};

export default Footer;
